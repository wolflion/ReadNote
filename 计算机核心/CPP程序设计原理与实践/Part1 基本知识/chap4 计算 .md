##  缘起
+ 20161020开始看，71/671

##  内容
+ 如何通过一系列指令来计算一个数值量（表达式）
+ 如何在可替代操作中进行选择（选择）
+ 如何对一系列数据进行重复计算（迭代）

###  4.1 计算

###  4.2 目标和工具
+ 程序员的任务就是将计算表示出来，并且做到： **正确 简单 高效。** 并且要注意上述顺序。
+ 一个看似矛盾的事实是，关注代码结构和“代码质量”是程序取得成功的最快途径。
+ *抽象和分治* 没看

###  4.3 表达式


###  4.5 函数

###  4.6 向量
+ **向量：** 是一组可以通过索引来访问的顺序存储的数据元素。 `vector<int> v(6); //vector of 6 ints`
+ 4.6.1 向量空间增长
    + `push_back()` 
    + `size()`
    + *向量与数组的区别*
        + 无需事先指定向量的大小
        + 可以往向量中添加任意多个元素   
+ 4.6.2 一个数值计算的例子 88/671

###  4.7 语言特性
+ 循环，选择语句，简单的算术运算，比较和逻辑运算，变量和函数，标准库函数

###  附言
+ **踏踏实实地努力学习如何写好程序。**